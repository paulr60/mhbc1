<h1>All photos</h1>

<div class="row">
	<div class="new-content-button pull-right">
		<%= link_to "New Photo", new_photo_path, class: "link_button" %>
		<br></br>
	</div>
</div>

<%= will_paginate %>

<% @show_id = false %>
<% @show_actions = true %>
<%= render @photos %>

<div class="row span9">
	<%= will_paginate %>
</div>