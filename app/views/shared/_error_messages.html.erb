<% if @errmsgs.any? %>
	<div id="error_explanation">
		<div class="alert alert-error">
			The form contains <%= pluralize(@errmsgs.count, "error") %>.
		</div>
		<ul>
		<% @errmsgs.each do |msg| %>
			<li>* <%= msg %></li>
		<% end %>
		</ul>
	</div>
<% end %>
